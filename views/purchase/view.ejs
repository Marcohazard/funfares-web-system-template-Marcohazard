view: function (req, res) {
    Purchase.findOne(req.params.id).exec( function(err, model) {
        if (model != null)
            return res.view('purchase/view', {'purchase': model});
        else
            return res.send("No such deal");
    });
},
