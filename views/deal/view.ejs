view: function (req, res) {
    Deal.findOne(req.params.id).exec( function(err, model) {
        if (model != null)
            return res.view('deal/view', {'deal': model});
        else
            return res.send("No such deal");
    });
},
